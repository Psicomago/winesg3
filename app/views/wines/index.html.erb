<p id="notice"><%= notice %></p>

<h1>Wines</h1>
<%= link_to 'New Wine', new_wine_path %>


<table class= "table">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>cepas</th>
      <th>Clasificación</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @wines.each do |wine| %>
      <tr>
        <td><%= wine.name %></td>
        
        <td>
        <ul>
            <% wine.strain_wines.each do |wine_strain|  %>
               <li>
                   <%= "#{wine_strain.strain.name} | #{wine_strain.percentage}%" %>
                   <%= link_to "Edit Percentage", edit_strain_wine_path(wine_strain) %>
                   <%= link_to 'Destroy', wine_strain, method: :delete, data: { confirm: 'Are you sure?' } %>
                </li>
            <% end %>
        </ul>
        </td>
        <td><%= wine.strain_wines.count > 1 ? "Ensamblaje" : "Vino"  %> </td>
        <td><%= link_to 'Show', wine %></td>
        <td><%= link_to 'Edit', edit_wine_path(wine) %></td>
        <td><%= link_to 'Destroy', wine, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Wine', new_wine_path %>
